this is a read me file
git init =initialize empty repository
git add . = .present folder, git add means save all the files in that folder to git hub
ssh key , secure connection between my account an vs code local and public repository
personl link to compute and github
git remove - v ,. checked how many remote repsoitories are there
git remote add origin https=we set origin point,  

after every changes we have to do git committ